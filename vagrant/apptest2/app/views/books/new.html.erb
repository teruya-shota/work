    <h2>New book</h2>
      <%= form_for(@book) do |f| %>
        <% if @book.errors.any? %>
           <div id="error_explanation">
           <h2><%= @book.errors.count %> error prohibited this book from being saved:</h2>

            <ul>
            <% @book.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
            </ul>
          </div>
        <% end %>
        <div class="field row">
          <%= f.label :Title %>
          <%= f.text_field :title %>
        </div>
        <div class="field row">
          <%= f.label :Opinion %>
          <%= f.text_area :body %>
        </div>
        <div class="actions">
          <%= f.submit "Create Book", class: "btn btn-primary col-xs-12" do%>
        </div>
      <% end %>